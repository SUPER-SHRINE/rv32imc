# RV32M 拡張命令

RV32M は、乗算 (Multiply) および除算 (Divide) に関する命令セットです。

## 実装状況

現在、本エミュレータでは RV32M 拡張命令は**未実装**です。将来的なサポートを予定しています。

### サポート予定の命令
- `MUL`, `MULH`, `MULHSU`, `MULHU`
- `DIV`, `DIVU`, `REM`, `REMU`
